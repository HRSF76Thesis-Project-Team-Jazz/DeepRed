---
engines:
  duplication:
    enabled: true
    checks:
      Similar code:
        enabled: false
    config:
      languages:
      - javascript
      NODE_ENV:
      - travis
  eslint:
    enabled: true
ratings:

exclude_paths:
- server/test/**.js